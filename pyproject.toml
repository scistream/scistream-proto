[tool.poetry]
name = "scistream-proto"
version = "0.1.0"
description = ""
authors = ["Flavio Castro <fcastro@anl.gov>"]
readme = "README.md"
packages = [{include = "scistream_proto"}]

[tool.poetry.dependencies]
python = "^3.9"
zmq = "^0.0.0"
transitions = "^0.9.0"
optparse-pretty = "^0.1.1"
fire = "^0.5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
